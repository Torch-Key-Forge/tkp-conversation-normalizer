# Privacy and Security

The repository contains no private account export, credentials, cookies, tokens, browser databases, source URLs, or personal filesystem paths.

Operators should run normalization locally and review outputs before publication. Conversation content can contain personal, confidential, or regulated information even when the normalizer itself is safe.

The tool reads source JSON and writes new derived outputs. It does not modify the source file.
